/**
 * Configuración de Tailwind CSS v4
 * 
 * NOTA: En Tailwind v4, la mayoría de la configuración se hace en CSS usando @theme
 * Este archivo es opcional pero puede ser útil para configuraciones adicionales.
 * 
 * Con @tailwindcss/vite, el plugin detecta automáticamente los archivos a procesar,
 * pero puedes especificar rutas adicionales aquí si es necesario.
 */

export default {
  // En Tailwind v4, darkMode se configura principalmente en CSS con @custom-variant
  // El plugin de Vite detecta automáticamente los archivos en src/ e index.html
}

